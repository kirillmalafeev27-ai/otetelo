import React from 'react';
import ModeIcon from './ModeIcon.jsx';
import { MODE_INFO } from '../utils/constants.js';

export default function ModeSelect({ onSelect, onBack }) {
  return null; // Integrated into MainMenu
}
